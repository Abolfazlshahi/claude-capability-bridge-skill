# Build-time and test-time dependencies for the Claude Capability Bridge.
#
# RUNTIME NOTE: the installed Skill needs nothing from this file. The optional
# plugin hook runtime (bootstrap/bridge_hook.py) is standard-library only and
# deliberately does NOT import PyYAML or anything else listed here, so a host
# without these packages still runs the hooks.
#
# PyYAML is used only by the build/validation scripts to read Skill and card
# frontmatter with a real YAML parser instead of regex. If it is absent, the
# scripts fall back to a documented, restricted subset parser and say so; a
# malformed or unsupported construct raises an explicit error instead of
# silently guessing.
PyYAML>=6.0
